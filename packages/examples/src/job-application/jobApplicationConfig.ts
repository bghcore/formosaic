import type { IFormConfig } from "@formosaic/core";
import config from "../../../../examples/configs/job-application.json";

/**
 * Job Application form configuration.
 * Loaded from the shared examples/configs JSON asset.
 */
export const jobApplicationConfig = config as unknown as IFormConfig;
