import type { IFormConfig } from "@formosaic/core";
import config from "../../../../examples/configs/expense-report.json";

/**
 * Expense Report form configuration.
 * Loaded from the shared examples/configs JSON asset.
 */
export const expenseReportConfig = config as unknown as IFormConfig;
